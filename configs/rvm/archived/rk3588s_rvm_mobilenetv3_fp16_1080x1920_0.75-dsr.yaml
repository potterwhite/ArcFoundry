# dsr=0.75 实验：1080p src → 810p 内部处理分辨率
# 基于 rk3588s_rvm_mobilenetv3_fp16_1080x1920_dsr05.yaml 修改

project:
  name: "rvm_mobilenetv3_fp16_1080x1920_dsr075_rk3588"
  output_dir: "./output/rvm_mobilenetv3_fp16_1080x1920_dsr075_rk3588_release"
  workspace_dir: "./workspace/tmp"

target:
  platform: "rk3588"

build:
  optimization_level: 2
  verbose: true
  pruning: false
  eval_memory: false

  quantization:
    enabled: false
    dtype: "asymmetric_quantized-8"
    dataset: "./data/calibration_list.txt"
    sampling_interval: 5

models:
  - name: "rvm_mobilenetv3_fp16_1080x1920_dsr075_rk3588"
    path: "models/downloads/rvm_mobilenetv3.onnx"
    url: "https://github.com/PeterL1n/RobustVideoMatting/releases/download/v1.0.0/rvm_mobilenetv3_fp32.onnx"
    preprocess:
      fix_dynamic_shape:
        enabled: true
        strict_override: true
      fold_constant_inputs:
        enabled: true
        inputs:
          downsample_ratio: [0.75]     # ← 关键变化
      fix_int64_type: false
      simplify: true
      extract_metadata: true
    normalization:
      mean_values: [[0, 0, 0]]
      std_values: [[255, 255, 255]]
    input_shapes:
      src: [1, 3, 1080, 1920]
      # dH = round(1080 * 0.75) = 810, dW = round(1920 * 0.75) = 1440
      # r1: stride-2  → ceil(810/2)=405,  ceil(1440/2)=720
      # r2: stride-4  → ceil(810/4)=203,  ceil(1440/4)=360
      # r3: stride-8  → ceil(810/8)=102,  ceil(1440/8)=180
      # r4: stride-16 → ceil(810/16)=51,  ceil(1440/16)=90
      r1i: [1, 16, 405, 720]
      r2i: [1, 20, 203, 360]
      r3i: [1, 40, 102, 180]
      r4i: [1, 64,  51,  90]